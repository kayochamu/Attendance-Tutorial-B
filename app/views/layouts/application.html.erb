<!DOCTYPE html>
<html>
  <head>
    
    
    <title><%= full_title(yield(:title)) %></title>　
   
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all',
                               'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application',
                               'data-turbolinks-track': 'reload' %>
    <%= render 'layouts/shim' %>
  </head>
  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      
      <% flash.each do |message_type, msg| %>  <!-- フラッシュメッセージをブーツストラップで表示する>
      
      　<!-- Bootstrapに用意されているクラスアラート警告メッセージ・エラーメッセージ（Flash） -->
        <div class="alert alert-<%= message_type %>"><%= msg %></div>　 <!-- alert alertパーシャルshared _error　keyとvalueをアラートで出力　>
        
      <% end %>
      
      <%= yield %>
      
      <%= debug(params) if Rails.env.development? %>  <!-- デバッグ用の情報を表示するparamsを確認したい時-->
       <!-- @userの中身を確認したい時 %= debug @user if Rails.env.development? % -->
    
    </div>
  </body>
</html>